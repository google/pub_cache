language: dart
dart:
  - dev
  - stable

jobs:
  include:
    - stage: test
      script: ./tool/travis.sh
      env: PUB_HOSTED_URL=https://pub.flutter-io.cn
    - stage: test
      script: ./tool/travis.sh

branches:
  only:
    - master
